[project]
name = "webrtc-audio-processing-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "meson>=1.10.0",
    "pybind11>=3.0.1",
    "setuptools>=80.9.0",
    "wheel>=0.45.1",
]
